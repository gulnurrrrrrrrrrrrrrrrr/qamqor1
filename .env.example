# PostgreSQL (Render: Internal Database URL or External URL)
DATABASE_URL="postgresql://user:password@host:5432/qamqor?sslmode=require"

# Next.js
NODE_ENV=production

# Admin account (seed + optional registration gate). Only this email can be platform admin.
ADMIN_EMAIL=admin@qamqor.kz
